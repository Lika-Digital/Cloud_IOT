[pytest]
testpaths = tests
# `.` puts the repo root on sys.path so tests can import guard_worker/ (the guard
# worker lives outside backend/ on purpose — see docs/guard_b1_design.md).
pythonpath = backend .
filterwarnings =
    ignore::DeprecationWarning
