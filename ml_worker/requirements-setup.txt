# Extra dependencies needed only for the one-time model export step.
# The main ml_worker container does NOT need these at runtime.
#
# Run with 64-bit Python 3.11+:
#   py -3.11 -m venv C:/temp/ml_export_env
#   C:/temp/ml_export_env/Scripts/pip install -r ml_worker/requirements-setup.txt
#   C:/temp/ml_export_env/Scripts/python ml_worker/setup_models.py

# PyTorch CPU-only (much smaller than CUDA variant: ~250 MB vs ~2 GB)
torch>=2.4.0
torchvision

# Hugging Face transformers (for RTDetrForObjectDetection + AutoModel)
transformers>=4.44.2

# ONNX runtime for post-export verification
onnxruntime>=1.17.0
